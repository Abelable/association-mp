<scroll-view class="low-list" scroll-x>
  <view class="low-item" wx:for="{{list}}" wx:key="index" ontap="navToN" data-id="{{item.id}}">
    <image class="cover" src="{{item.image}}" mode="aspectFill" lazy-load />
    <view class="title">{{item.name}}</view>
  </view>
</scroll-view>
